// Classe privée pour une palanquée

class dive_team {
    constructor() {
        this.max_depth = max_depth;
        this.max_duration = max_duration;
        this.actual_depth = actual_depth;
        this.actual_duration = actual_duration;
        this.dive_type = dive_type;
        this.sequence_number = sequence_number;
        this.start_time = start_time;
        this.stop_time = stop_time;
        this.comment = comment;
    
    }

    get max_depth() {
        return this.max_depth;
    }

    set max_depth(max_depth) {
        this.max_depth = max_depth;
    }

    get max_duration() {
        return this.max_duration;
    }

    set max_duration(max_duration) {
        this.max_duration = max_duration;
    }

    get actual_depth() {
        return this.actual_depth;
    }

    set actual_depth(actual_depth) {
        this.actual_depth = actual_depth;
    }

    get actual_duration() {
        return this.actual_duration;
    }

    set actual_duration(actual_duration) {
        this.actual_duration = actual_duration;
    }

    get dive_type() {
        return this.dive_type;
    }

    set dive_type(dive_type) {
        this.dive_type = dive_type;
    }

    get sequence_number() {
        return this.sequence_number;
    }

    set sequence_number(sequence_number) {
        this.sequence_number = sequence_number;
    }

    get start_time() {
        return this.start_time;
    }

    set start_time(start_time) {
        this.start_time = start_time;
    }

    get stop_time() {
        return this.stop_time;
    }

    set stop_time(stop_time) {
        this.stop_time = stop_time;
    }

    get comment() {
        return this.comment;
    }
    
    set comment(comment) {
        this.comment = comment;
    }
}


